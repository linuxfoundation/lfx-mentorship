# Copyright The Linux Foundation and each contributor to LFX.
# SPDX-License-Identifier: MIT
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "lfx-mentorship-frontend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lfx-mentorship-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "lfx-mentorship-frontend.selectorLabels" . | nindent 4 }}
