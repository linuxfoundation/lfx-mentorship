# Copyright The Linux Foundation and each contributor to LFX.
# SPDX-License-Identifier: MIT
{{- if .Values.fundingStatsSyncCronJob.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "lfx-v2-mentorship-service.fullname" . }}-funding-stats-sync
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lfx-v2-mentorship-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: program-funding-stats-sync
spec:
  schedule: {{ .Values.fundingStatsSyncCronJob.schedule | quote }}
  concurrencyPolicy: {{ .Values.fundingStatsSyncCronJob.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.fundingStatsSyncCronJob.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.fundingStatsSyncCronJob.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/component: program-funding-stats-sync
        spec:
          restartPolicy: OnFailure
          serviceAccountName: {{ include "lfx-v2-mentorship-service.serviceAccountName" . }}
          automountServiceAccountToken: false
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          containers:
            - name: program-funding-stats-sync
              securityContext:
                {{- toYaml .Values.securityContext | nindent 16 }}
              image: "{{ .Values.fundingStatsSyncCronJob.image.repository | default .Values.image.repository }}:{{ .Values.fundingStatsSyncCronJob.image.tag | default (required "image.tag is required" .Values.image.tag) }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["/app/program-funding-stats-sync"]
              {{- with .Values.env }}
              env:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              envFrom:
                - configMapRef:
                    name: {{ include "lfx-v2-mentorship-service.fullname" . }}-config
                - secretRef:
                    name: {{ .Values.secretName }}
              resources:
                {{- toYaml .Values.fundingStatsSyncCronJob.resources | nindent 16 }}
{{- end }}
