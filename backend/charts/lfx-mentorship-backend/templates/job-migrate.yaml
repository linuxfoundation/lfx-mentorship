# Copyright The Linux Foundation and each contributor to LFX.
# SPDX-License-Identifier: MIT
#
# Applies pending db/migrations before the upgraded app pods start.
# hook-weight ensures this runs after the placeholder Secret (weight -5) but
# before the Deployment (unweighted, i.e. weight 0), and each release gets its
# own Job name so Helm doesn't try to reuse a completed one.
#
# Deliberately uses the namespace's default ServiceAccount and does not mount
# the app ConfigMap: on a first `helm install`, pre-install hooks run before
# the chart's own ServiceAccount and ConfigMap exist, so referencing either
# here would leave the hook pod stuck (unadmittable / CreateContainerConfigError).
# The migrator only needs the database connection variables (from the Secret /
# env: below) and makes no Kubernetes API calls, so neither is required.
{{- if .Values.migrationJob.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ printf "%s-migrate-%v" (include "lfx-v2-mentorship-service.fullname" .) .Release.Revision | trunc 63 | trimSuffix "-" }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lfx-v2-mentorship-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: db-migrate
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-1"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  backoffLimit: {{ .Values.migrationJob.backoffLimit }}
  activeDeadlineSeconds: {{ .Values.migrationJob.activeDeadlineSeconds }}
  template:
    metadata:
      labels:
        app.kubernetes.io/component: db-migrate
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: migrate
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ required "image.tag is required" .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/app/migrate"]
          {{- with .Values.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          envFrom:
            - secretRef:
                name: {{ .Values.secretName }}
          resources:
            {{- toYaml .Values.migrationJob.resources | nindent 12 }}
{{- end }}
